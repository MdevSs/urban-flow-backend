.env (example)
ADMIN_CREATE_TOKEN=some-short-lived-secret
JWT_SECRET=your_jwt_secret
JWT_EXPIRES_IN=3600s

Commands:
npm install @nestjs/common @nestjs/core @nestjs/jwt @nestjs/passport passport passport-jwt bcrypt prisma @prisma/client
# initialize prisma separately and run `npx prisma generate`